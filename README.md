# INFO088-Trabajo-grupal de Vicente Osorio

Para ocupar el programa, una vez ejecutado, ocupar las siguientes funciones para testear elcodigo

testSolucion1(ArregloD1, D2Insertar, D2Eliminar, D2Buscar, static_cast<int>(ArregloD1.size()));

testSolucion2(ArregloD1, D2Insertar, D2Eliminar, D2Buscar); (notar que se entrega k una vez se inicia el codigo)

#IMPORTANTE
El codigo limpia memoria una vez se corre un test, por ende cada vez que se haga un test se debe volver a iniciar el main.cpp